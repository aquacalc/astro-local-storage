---
import Layout from "../layouts/Layout.astro";

import Test from "../components/Test.svelte";
---

<Layout>
  <p>
    This <code>.astro</code> file (<code>/pages/good-to-go.astro</code>) imports <code>Test.svelte</code>
  </p>

  <code style="margin-left: 3rem;">
    import Test from "../components/Test.svelte";
  </code>

  <p>
    uses the <a
      href="https://docs.astro.build/en/reference/directives-reference/#clientonly"
      target="_blank"
      rel="noopener noreferrer">client:only</a
    > directive
  </p>

  <code style="margin-left: 3rem;">
    &#60;Test client:only /&#62;
  </code>

  <p>
    and persists entered data to local storage. No problems.
  </p>

  <hr />

  <Test client:only />
</Layout>
